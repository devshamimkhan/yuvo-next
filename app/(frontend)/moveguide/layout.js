/**
 * Pass-through layout for the /moveguide segment.
 * StoreHeader, StoreFooter, and StoreStyles are inherited from the parent
 * app/(store)/layout.js — they stay mounted across ALL storefront navigation.
 */
export default function MoveGuideLayout({ children }) {
  return children;
}
